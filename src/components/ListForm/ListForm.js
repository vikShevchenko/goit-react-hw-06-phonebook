// import React from 'react'
// import { removeFilter} from "../../redux/userSlise";
// import { useDispatch } from "react-redux";

// export const ListForm = ({search}) => {

//     const dispatch = useDispatch();
//   return (
//     <ul className="cont_list">
//       {search.map((item) => (
//         <li style={{ listStyle: "none" }} key={item.id}> {item.name}: {item.number} <button onClick={() => dispatch(removeFilter(item.id))}>Dell</button></li>
//       ))}
//     </ul>
    
//   )
// }
